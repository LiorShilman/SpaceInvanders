# shared (Phase 1+)

טיפוסים וקבועים המשותפים בין `client` ל-`server` — למשל צורת ה-state schema של Colyseus, טיפוסי הודעות רשת. נוצר כשה-server מתחיל להתממש, כדי שלא יהיה שכפול/סחיפה (drift) בין הגדרות הצד השני.
