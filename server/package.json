{
  "name": "server",
  "private": true,
  "description": "Colyseus authoritative server — Phase 1 (Co-op Network). Currently a proof-of-concept slice: one room that relays each connected ship's position/rotation to every other client. Not yet the full server-authoritative simulation described in server/README.md (enemies/waves/bosses still run entirely client-side, per player) — see co-op-survival.ts's own comment for the exact scope and what's still missing before that's true.",
  "type": "module",
  "scripts": {
    "dev": "tsx watch src/index.ts",
    "build": "tsc -b",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@colyseus/schema": "^3.0.76",
    "@colyseus/ws-transport": "0.16.5",
    "colyseus": "0.16.5",
    "cors": "^2.8.5",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.2",
    "tsx": "^4.19.2",
    "typescript": "^5.7.2"
  }
}
